--- a/net/minecraft/core/registries/BuiltInRegistries.java
+++ b/net/minecraft/core/registries/BuiltInRegistries.java
@@ -414,6 +414,7 @@ public class BuiltInRegistries {
         createContents();
         runnable.run(); // Paper
         freeze();
+        cloud.victus.core.runtime.FabricRegistryBridge.afterBootstrap(); // Victus - Fabric registry sync fallback for Paper bootstrap shape
         validate(REGISTRY);
     }
 
