--- a/net/minecraft/server/MinecraftServer.java
+++ b/net/minecraft/server/MinecraftServer.java
@@ -1007,3 +_,4 @@
     protected void stopServer() {
+        cloud.victus.core.runtime.FabricLifecycleBridge.stopping(this); // Victus - Fabric lifecycle fallback for Paper startup shape
         if (Thread.currentThread() == this.serverThread) this.executeAllRecentInternalTasks(); // Paper - execute tasks on stop
         // CraftBukkit start - prevent double stopping on multiple threads
@@ -1097,3 +_,4 @@
         // Paper start - Improved watchdog support - move final shutdown items here
         Util.shutdownExecutors();
         this.onServerExit();
+        cloud.victus.core.runtime.FabricLifecycleBridge.stopped(this); // Victus - Fabric lifecycle fallback for Paper startup shape
@@ -1265,3 +_,4 @@
     protected void runServer() {
         try {
+            cloud.victus.core.runtime.FabricLifecycleBridge.starting(this); // Victus - Fabric lifecycle fallback for Paper startup shape
             if (!this.initServer()) {
@@ -648,3 +_,4 @@
         // Paper end - Configurable player collision; Handle collideRule team for player collision toggle
+        cloud.victus.core.runtime.HybridBukkitHost.bind(this); // Victus - bind optional Fabric/Bukkit bridge before POSTWORLD plugin callbacks
         this.server.enablePlugins(org.bukkit.plugin.PluginLoadOrder.POSTWORLD);
         this.server.spark.registerCommandBeforePlugins(this.server); // Paper - spark
@@ -1272,3 +_,4 @@
             this.initTickSchedule(); // Paper - improve tick loop
             this.statusIcon = this.loadStatusIcon().orElse(null);
             this.status = this.buildServerStatus();
+            cloud.victus.core.runtime.FabricLifecycleBridge.started(this); // Victus - Fabric lifecycle fallback for Paper startup shape
@@ -1291,3 +_,5 @@
             LOGGER.info("Done ({})! For help, type \"help\"", String.format(java.util.Locale.ROOT, "%.3fs", actualDoneTimeMs / 1000.00D)); // Paper - Improve startup message
+            cloud.victus.engine.VictusEngine.logStartupBanner(); // Victus - native post-ready branding, guarded once
+            cloud.victus.core.runtime.HybridBukkitHost.ready(); // Victus - bridge READY follows genuine Paper Done marker
             org.spigotmc.WatchdogThread.tick();
             // Paper end
@@ -1580,6 +_,8 @@
     protected void tickServer(final BooleanSupplier haveTime) {
         org.spigotmc.WatchdogThread.tick(); // Spigot
         long nano = Util.getNanos();
+        cloud.victus.engine.VictusTickTimings.beginTick(); // Victus - per-subsystem tick timing
+        net.minecraft.network.protocol.game.ClientboundLevelChunkPacketData.maybeSelfTest(this); // Victus - async chunk-send byte-equivalence gate
         int emptyTickThreshold = this.pauseWhenEmptySeconds() * 20;
         this.removeDisabledPluginsBlockingSleep(); // Paper - API to allow/disallow tick sleeping
         if (emptyTickThreshold > 0) {
@@ -1661,6 +_,7 @@
         this.server.spark.tickEnd(((double)(endTime - this.currentTickStart) / 1000000D)); // Paper - spark
         profiler.push("tallying");
         long tickTime = Util.getNanos() - nano;
+        cloud.victus.engine.VictusTickTimings.endTick(tickTime); // Victus - per-subsystem tick timing
         int tickIndex = this.tickCount % 100;
         this.aggregatedTickTimesNanos = this.aggregatedTickTimesNanos - this.tickTimesNanos[tickIndex];
         this.aggregatedTickTimesNanos += tickTime;
@@ -1812,6 +_,35 @@
         this.updateEffectiveRespawnData();
 
         this.isIteratingOverLevels = true; // Paper - Throw exception on world create while being ticked
+        // Victus start - PARALLEL tick engine (EXPERIMENTAL; opt-in threading.mode=parallel). Default SINGLE
+        // takes the original serial loop in the else branch below (byte-identical, zero behaviour change).
+        if (cloud.victus.engine.VictusEngine.parallelWorldTicking()) {
+            java.util.List<ServerLevel> victusLevels = new java.util.ArrayList<>();
+            for (ServerLevel level : this.getAllLevels()) victusLevels.add(level);
+            // Serial pre-pass on the MAIN thread for plugin-visible + static per-world state (safe), THEN tick
+            // the worlds in parallel. skipHopperEvents is a single static shared across worlds, so it cannot be
+            // per-world under parallel ticking — set once CONSERVATIVELY (fire events unless nobody listens;
+            // per-world hopper.disableMoveEvent is not honoured in parallel — see docs/phase-4).
+            final boolean victusPhysics = org.bukkit.event.block.BlockPhysicsEvent.getHandlerList().getRegisteredListeners().length > 0;
+            final boolean victusEntityMove = io.papermc.paper.event.entity.EntityMoveEvent.getHandlerList().getRegisteredListeners().length > 0;
+            for (ServerLevel level : victusLevels) {
+                level.hasPhysicsEvent = victusPhysics;
+                level.hasEntityMoveEvent = victusEntityMove;
+                level.updateLagCompensationTick();
+            }
+            net.minecraft.world.level.block.entity.HopperBlockEntity.skipHopperEvents = org.bukkit.event.inventory.InventoryMoveItemEvent.getHandlerList().getRegisteredListeners().length == 0;
+            final java.util.function.BooleanSupplier victusHaveTime = haveTime;
+            cloud.victus.engine.VictusEngine.tickWorldsParallel(victusLevels, level -> {
+                try {
+                    level.tick(victusHaveTime);
+                } catch (Throwable t) {
+                    CrashReport report = CrashReport.forThrowable(t, "Exception ticking world");
+                    level.fillReportDetails(report);
+                    throw new ReportedException(report);
+                }
+                level.explosionDensityCache.clear(); // Paper - Optimize explosions
+            });
+        } else {
         for (ServerLevel level : this.getAllLevels()) {
             level.hasPhysicsEvent = org.bukkit.event.block.BlockPhysicsEvent.getHandlerList().getRegisteredListeners().length > 0; // Paper - BlockPhysicsEvent
             level.hasEntityMoveEvent = io.papermc.paper.event.entity.EntityMoveEvent.getHandlerList().getRegisteredListeners().length > 0; // Paper - Add EntityMoveEvent
@@ -1832,6 +_,8 @@
             profiler.pop();
             level.explosionDensityCache.clear(); // Paper - Optimize explosions
         }
+        }
+        // Victus end
         this.isIteratingOverLevels = false; // Paper - Throw exception on world create while being ticked
 
         profiler.popPush("connection");
