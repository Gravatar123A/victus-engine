--- a/net/minecraft/server/level/ServerLevel.java
+++ b/net/minecraft/server/level/ServerLevel.java
@@ -881,6 +_,7 @@
                 profiler.pop();
             }
 
+            long __veEnt = System.nanoTime(); // Victus - tick timing
             io.papermc.paper.entity.activation.ActivationRange.activateEntities(this); // Paper - EAR
             this.entityTickList
                 .forEach(
@@ -908,9 +_,10 @@
                         }
                     }
                 );
+            cloud.victus.engine.VictusTickTimings.add(cloud.victus.engine.VictusTickTimings.ENTITIES, System.nanoTime() - __veEnt); // Victus - tick timing
             if (this.paperConfig().unsupportedSettings.ticking.blockEntities) { // Paper - option to disable ticking
             profiler.popPush("blockEntities");
-            this.tickBlockEntities();
+            long __veBE = System.nanoTime(); this.tickBlockEntities(); cloud.victus.engine.VictusTickTimings.add(cloud.victus.engine.VictusTickTimings.BLOCK_ENTITIES, System.nanoTime() - __veBE); // Victus - tick timing
             profiler.pop();
             } // Paper - option to disable ticking
             this.spigotConfig.currentPrimedTnt = 0; // Spigot
