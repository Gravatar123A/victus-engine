--- a/net/minecraft/server/network/PlayerChunkSender.java
+++ b/net/minecraft/server/network/PlayerChunkSender.java
@@ -81,7 +_,38 @@
     public static void sendChunk(final ServerGamePacketListenerImpl connection, final ServerLevel level, final LevelChunk chunk) { // Paper - rewrite chunk system - public
         // Paper start - Anti-Xray
         final boolean shouldModify = level.chunkPacketBlockController.shouldModify(connection.player, chunk);
-        connection.send(new ClientboundLevelChunkWithLightPacket(chunk, level.getLightEngine(), null, null, shouldModify));
+        // Victus start - async chunk send (v1: non-Anti-Xray only; snapshot-on-main → serialize-on-worker → in-order via Paper's isReady FIFO)
+        if (!shouldModify && net.minecraft.network.protocol.game.ClientboundLevelChunkPacketData.asyncEnabled()) {
+            final ChunkPos cpos = chunk.getPos();
+            final net.minecraft.network.protocol.game.ClientboundLevelChunkPacketData.Snapshot snap =
+                    net.minecraft.network.protocol.game.ClientboundLevelChunkPacketData.capture(chunk); // MAIN: freeze palette/heightmaps/block-entities
+            final net.minecraft.network.protocol.game.ClientboundLightUpdatePacketData lightData =
+                    new net.minecraft.network.protocol.game.ClientboundLightUpdatePacketData(cpos, level.getLightEngine(), null, null); // MAIN: light read
+            final ClientboundLevelChunkWithLightPacket shell = ClientboundLevelChunkWithLightPacket.deferred(cpos, lightData);
+            connection.send(shell); // MAIN: reserve the per-player FIFO slot IN ORDER (ready=false holds later packets behind it)
+            final net.minecraft.server.MinecraftServer srv = level.getServer();
+            net.minecraft.network.protocol.game.ClientboundLevelChunkPacketData.submitAsync(() -> {
+                net.minecraft.network.protocol.game.ClientboundLevelChunkPacketData data = null;
+                try {
+                    data = net.minecraft.network.protocol.game.ClientboundLevelChunkPacketData.fromSnapshot(snap); // WORKER: serialize from frozen snapshot
+                } catch (Throwable t) {
+                    org.slf4j.LoggerFactory.getLogger("Victus").warn("async chunk serialize failed; main-thread fallback: " + t);
+                }
+                if (data != null) {
+                    shell.setChunkData(data);
+                    shell.setReady(true); // publishes chunkData (happens-before) + releases the FIFO
+                } else {
+                    srv.execute(() -> { // R7: rebuild on main from the same safe snapshot, then release
+                        try { shell.setChunkData(net.minecraft.network.protocol.game.ClientboundLevelChunkPacketData.fromSnapshot(snap)); }
+                        catch (Throwable t2) { org.slf4j.LoggerFactory.getLogger("Victus").warn("async chunk fallback failed: " + t2); }
+                        shell.setReady(true);
+                    });
+                }
+            });
+        } else {
+            connection.send(new ClientboundLevelChunkWithLightPacket(chunk, level.getLightEngine(), null, null, shouldModify));
+        }
+        // Victus end
         // Paper end - Anti-Xray
         // Paper start - PlayerChunkLoadEvent
         if (io.papermc.paper.event.packet.PlayerChunkLoadEvent.getHandlerList().getRegisteredListeners().length > 0) {
